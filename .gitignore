
# Ignore Terraform state files (store sensitive info like AWS keys)
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfvars
.terraform.lock.hcl
terraform.tfstate.d/
output.tfstate
*.tfplan

# Private business strategy (DO NOT COMMIT)
BUSINESS-STRATEGY-PRIVATE.md

# Node.js (package.json detected)
node_modules/
package-lock.json

# Python virtual environments (if using venv or similar)
env/
venv/
*.env
test.env

# Ignore Python dependencies (if any)
__pycache__/
*.pyc
*.pyo
*.pyd

# Ignore AWS credentials (VERY IMPORTANT)
.aws/
credentials
config
# Ignore VS Code settings (optional)
.vscode/
# Ignore logs and temporary files
*.log
*.tmp
.DS_Store
aws_dev_workflow.sh

# IDE/editor files
.idea/
*.swp

# Gitleaks binaries & archives
gitleaks
gitleaks.tar.gz
gitleaks-linux-arm64.tar.gz

# key files — NEVER commit private keys
# These files contain sensitive information and must NOT be in version control.
*.pem
kali-key
kali-key.pub
kali.sh
phantomwall-keypair.pem
phantomwall_honeypot_key.pem
phantomwall_kali_key.pem

# Lambda deployment zips (binary artifacts, rebuilt by Terraform)
lambda/*.zip
lambda/**/*.zip

# Temporary/cleanup artifacts
delobj.json

# Ignore any other files or directories that are not relevant to the project
modules/
output.json
opencanary.sh
s3_event.tf
sagemaker-training-config.json
codes/